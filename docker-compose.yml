version: '3.4'
services:
  k6:
    image: loadimpact/k6:latest
    command: run -e DOMAIN=https://cdn.speedsize.com -e IMAGETYPE=speedsize-url-9 -e FLATTEN=/fltn /scripts/test.js
    volumes:
      - ./scripts:/scripts 
